spring:
  application:
    name: auth-service

  datasource:
    username: postgres
    password: admin
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/auth-service

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

jwt:
  token:
    access:
      secret: "PkO9ZWWsRm-SMi1yM26jl-DvczK1I2JP"
      expiration: 3600000
    refresh:
      secret: "MBMMtbXUpb-7u01Pnhw0p-8zE9Kli0DU"
      expiration: 604800000

server:
  port: 0

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true